# Google Sheets Webhook URL for real-time data export
# To set up Google Sheets integration:
# 
# 1. Create a new Google Sheet and note the Sheet ID from the URL
# 2. Open the Sheet and go to "Extensions" > "Apps Script"
# 3. Replace the code with this template:
#
#    function doPost(e) {
#      const sheet = SpreadsheetApp.openById('YOUR_SHEET_ID');
#      const ws = sheet.getActiveSheet();
#      const data = JSON.parse(e.postData.contents);
#      const row = [
#        data.data.uid,
#        data.data.name,
#        data.data.mobile,
#        data.data.age,
#        data.data.date,
#        data.data.referral || '',
#        data.type === 'bill' ? data.data.total : '',
#        data.data.timestamp
#      ];
#      ws.appendRow(row);
#      return ContentService.createTextOutput('{"success": true}').setMimeType(ContentService.MimeType.JSON);
#    }
#
# 4. Save and click "Deploy" > "New Deployment"
# 5. Choose "Web app" and set "Execute as" to your email
# 6. Set "Who has access" to "Anyone"
# 7. Copy the deployment URL and paste it below
#
# 8. Replace YOUR_SHEET_ID with your actual Sheet ID from the URL
#    (format: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit)

VITE_GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/d/YOUR_DEPLOYMENT_ID/usercopy
